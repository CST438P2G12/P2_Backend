{
  "id": "gymlog-environment",
  "name": "GymLog Environment",
  "values": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8080",
      "type": "default",
      "enabled": true,
      "description": "Change this to your deployed URL when testing against production. e.g. https://your-app.herokuapp.com"
    }
  ],
  "_postman_variable_scope": "environment",
  "info": {
    "description": "Environment for the GymLog API. Switch the baseUrl value between http://localhost:8080 (local) and your deployed server URL (production)."
  }
}